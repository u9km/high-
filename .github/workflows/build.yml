name: Ultimate UEDumper Builder (Fix dm.pl & No-Jailbreak)

on:
  push:
    branches: [ "**" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: 1. Checkout Code
        uses: actions/checkout@v4

      # 1) تثبيت أدوات النظام الضرورية لحل مشكلة dm.pl
      - name: 2. Install System Tools (Fix dm.pl)
        run: |
          brew update
          # تثبيت dpkg هو الحل الجذري لرسالة الخطأ التي تظهر في الصورة
          brew install ldid dpkg make 

      # 2) فك الضغط والبحث الذكي عن مجلد المشروع
      - name: 3. Smart Unzip & Locate
        run: |
          find . -name "*.zip" -exec unzip -o -q {} \;
          # البحث عن أي ملف برمجي لتحديد مجلد المشروع (cpp, mm, xm)
          SOURCE_FILE=$(find . -type f \( -name "*.xm" -o -name "*.mm" -o -name "*.cpp" -o -name "*.c" \) | head -n 1)
          if [ -z "$SOURCE_FILE" ]; then
            echo "::error::لم يتم العثور على سورس كود داخل الملف!"
            exit 1
          fi
          PROJECT_DIR=$(dirname "$SOURCE_FILE")
          echo "PROJECT_DIR=$PROJECT_DIR" >> $GITHUB_ENV
          echo "✅ Project found in: $PROJECT_DIR"

      # 3) تحميل المكتبات الضرورية (KittyMemory, fmt, Keystone)
      - name: 4. Install Dependencies
        run: |
          cd ${{ env.PROJECT_DIR }}
          mkdir -p deps
          git clone --depth=1 https://github.com/MJx0/KittyMemory.git deps/KittyMemory
          git clone --depth=1 https://github.com/fmtlib/fmt.git deps/fmt
          git clone --depth=1 https://github.com/keystone-engine/keystone.git deps/keystone

      # 4) إعادة كتابة Makefile و Control لضمان البناء الصحيح
      - name: 5. Generate Build Configs
        run: |
          cd ${{ env.PROJECT_DIR }}
          rm -f Makefile control
          
          cat > Makefile <<EOF
          TARGET := iphone:clang:latest:14.0
          ARCHS = arm64
          include \$(THEOS)/makefiles/common.mk
          TWEAK_NAME = UEDumper
          UEDumper_FILES = \$(wildcard *.xm) \$(wildcard *.mm) \$(wildcard *.cpp) \$(wildcard src/*.cpp) \$(wildcard src/*.mm)
          UEDumper_CCFLAGS = -std=c++17 -I"deps/KittyMemory" -I"deps/fmt/include" -I"deps/keystone/include" -DFMT_HEADER_ONLY
          UEDumper_CFLAGS = -I"deps/KittyMemory" -I"deps/fmt/include" -I"deps/keystone/include"
          include \$(THEOS_MAKE_PATH)/tweak.mk
          EOF

          cat > control <<EOF
          Package: com.uedumper.ios
          Name: UEDumper
          Version: 1.0
          Architecture: iphoneos-arm
          Description: UEDumper for PUBG (Non-Jailbreak)
          Maintainer: Dev
          Author: Dev
          Section: Tweaks
          EOF

      # 5) إعداد بيئة Theos
      - name: 6. Setup Theos
        uses: actions/checkout@v4
        with:
          repository: theos/theos
          path: theos
          ref: master

      # 6) البناء واستخراج الـ Dylib للحقن (بدون جلبريك)
      - name: 7. Build & Extract Result
        run: |
          export THEOS=$(pwd)/theos
          export PATH=$THEOS/bin:$PATH
          cd ${{ env.PROJECT_DIR }}
          make clean
          # تفعيل خيار FINALPACKAGE لتقليل حجم الملف الناتج
          make package FINALPACKAGE=1 messages=yes
          
          mkdir -p ../FINAL_OUT
          # استخراج ملف الـ dylib الصافي المطلوب للحقن اليدوي
          find .theos/obj -name "*.dylib" -exec cp {} ../FINAL_OUT/UEDumper_Injection.dylib \;
          # نسخ ملف الـ DEB الناتج أيضاً
          cp packages/*.deb ../FINAL_OUT/ 2>/dev/null || true

      - name: 8. Upload Result
        uses: actions/upload-artifact@v4
        with:
          name: UEDumper_Ready_To_Inject
          path: FINAL_OUT/*
