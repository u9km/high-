name: Test Workflow Visible

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: macos-latest
    steps:
      - name: Say Hello
        run: echo "✅ Workflow is visible and working"
